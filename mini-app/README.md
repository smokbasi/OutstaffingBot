# OutstaffingBot Mini App

React 18 + Vite + TypeScript skeleton. Telegram Web App SDK placeholder.

## Setup

```powershell
cd mini-app
npm install
npm run dev
```

Open via Telegram bot WebApp button (`MINI_APP_URL` in `.env`) for full initData flow (Phase 1).
